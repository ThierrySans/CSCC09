setTimeout(function(){ 
    console.info('1. 5s timeout');
}, 1000);

setTimeout(function(){ 
    console.info('3. 1s timeout');
}, 1000);

// a.sort();
// console.info('3. array sorted');

console.info('4. done');
